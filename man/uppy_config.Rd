% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uppy-config.R
\name{uppy_config}
\alias{uppy_config}
\title{Configure Uppy file restrictions and dashboard settings}
\usage{
uppy_config(
  max_file_size = NULL,
  max_number_of_files = NULL,
  min_number_of_files = NULL,
  allowed_file_types = NULL,
  height = 350,
  width = "100\%",
  inline = TRUE,
  theme = c("light", "dark", "auto"),
  note = NULL,
  auto_proceed = FALSE,
  show_progress_details = TRUE,
  show_remove_button_after_complete = TRUE,
  hide_upload_button = NULL,
  proudly_display_powered_by_uppy = FALSE
)
}
\arguments{
\item{max_file_size}{Maximum file size in bytes. Default is NULL (no limit).}

\item{max_number_of_files}{Maximum number of files that can be uploaded.
Default is NULL (no limit).}

\item{min_number_of_files}{Minimum number of files required. Default is NULL.}

\item{allowed_file_types}{Character vector of allowed file types/extensions
(e.g., \code{c(".pdf", ".docx", "image/*")}). Default is NULL (all types allowed).}

\item{height}{Dashboard height in pixels. Default is 350.}

\item{width}{Dashboard width. Can be numeric (pixels) or character ("100\%").
Default is "100\%".}

\item{inline}{Logical. Whether to show the dashboard inline (TRUE) or as a
modal (FALSE). Default is TRUE.}

\item{theme}{Character. Dashboard theme: "light", "dark", or "auto". Default is "light".}

\item{note}{Character. Note text to display in the dashboard. Default is NULL.}

\item{auto_proceed}{Logical. Whether to automatically process files after they
are selected. When TRUE, files are immediately available in Shiny.
When FALSE (default, recommended), user must click the "Upload" button to batch-process files.
Default is FALSE for stability with large file counts.}

\item{show_progress_details}{Logical. Show detailed progress information.
Default is TRUE.}

\item{show_remove_button_after_complete}{Logical. Show remove button after
upload completes. Default is TRUE.}

\item{hide_upload_button}{Logical. Hide the upload button. Default is FALSE.}

\item{proudly_display_powered_by_uppy}{Logical. Show "Powered by Uppy" badge.
Default is FALSE.}
}
\value{
A list of configuration options for Uppy
}
\description{
Helper function to configure Uppy's core settings including file restrictions,
dashboard appearance, and behavior.
}
\examples{
\dontrun{
uppy_config(
  max_file_size = 50 * 1024 * 1024,  # 50MB
  allowed_file_types = c(".pdf", ".docx"),
  height = 400,
  note = "Upload your documents here"
)
}
}
