% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uppy-server.R
\name{uppy_process_files}
\alias{uppy_process_files}
\title{Process Uppy uploaded files on the server}
\usage{
uppy_process_files(files_data, temp_dir = tempdir())
}
\arguments{
\item{files_data}{List of file data from Uppy (typically from \verb{input$<id>})}

\item{temp_dir}{Character. Directory to write temporary files. Default is \code{tempdir()}.}
}
\value{
A data frame with columns: name, size, type, datapath (matching \code{fileInput()} format)
}
\description{
Helper function to process files uploaded via Uppy and convert them to
a format compatible with standard Shiny file handling. This function
decodes base64-encoded file data and writes files to temporary locations.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  observeEvent(input$files, {
    # Files are automatically processed by the input binding
    # but you can use this function if you need custom processing
    files_df <- uppy_process_files(input$files)
    print(files_df)
  })
}
}
}
