% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uppy-transloadit.R
\name{uppy_plugins}
\alias{uppy_plugins}
\title{Configure Uppy plugins for cloud storage and other sources}
\usage{
uppy_plugins(
  dropbox = NULL,
  google_drive = NULL,
  onedrive = NULL,
  url = FALSE,
  webcam = FALSE,
  audio = FALSE,
  screen_capture = FALSE
)
}
\arguments{
\item{dropbox}{Configuration for Dropbox plugin. Use \code{uppy_transloadit()} to
configure with Transloadit Companion, or FALSE to disable. Default is NULL (disabled).}

\item{google_drive}{Configuration for Google Drive plugin. Use \code{uppy_transloadit()}
or a custom config list, or FALSE to disable. Default is NULL (disabled).}

\item{onedrive}{Configuration for OneDrive plugin. Use \code{uppy_transloadit()}
or a custom config list, or FALSE to disable. Default is NULL (disabled).}

\item{url}{Logical or list. Enable URL import plugin. Default is FALSE.}

\item{webcam}{Logical or list. Enable webcam capture. Default is FALSE.}

\item{audio}{Logical or list. Enable audio recording. Default is FALSE.}

\item{screen_capture}{Logical or list. Enable screen capture. Default is FALSE.}
}
\value{
A list of plugin configurations
}
\description{
Helper function to enable and configure Uppy plugins for various file sources
including cloud storage providers (Dropbox, Google Drive, OneDrive) and
other input methods (webcam, URL import, etc.).
}
\examples{
\dontrun{
# Enable Dropbox with Transloadit
uppy_plugins(
  dropbox = uppy_transloadit(
    key = "YOUR_KEY",
    credentials_name = "my-dropbox"
  )
)

# Enable multiple plugins
uppy_plugins(
  dropbox = uppy_transloadit(key = "KEY1", credentials_name = "dropbox-creds"),
  google_drive = uppy_transloadit(key = "KEY1", credentials_name = "gdrive-creds"),
  url = TRUE,
  webcam = TRUE
)
}
}
